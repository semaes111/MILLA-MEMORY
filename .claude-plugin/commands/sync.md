---
description: Sync palace with source files — detect stale/missing drawers and re-mine changed files.
argument-hint: Optional directory to limit sync scope. Use --force to re-mine, --dry-run to preview.
allowed-tools: Bash, Read
---

Invoke the generic mempalace skill (using the Skill tool) with the `sync` command, then follow its instructions.
